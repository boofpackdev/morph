# Plan Mode: PRD → Architecture Plan

Take the current Spark output and create a full architecture plan.

Run `/morph:plan` to generate the architecture diagram, data models, component tree, and task DAG.
